public class Bar {
    private static int y = 2;
    private static int x = 1;

    public int subtract() {
        return Bar.x - Bar.y;
    }

    public int sum() {
        return Bar.x + Bar.y;
    }
}
